#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);

using namespace std;
typedef std::vector<int> vi;
map<vi,int> DP;
int recurse(vi v){
	if(DP.find(v)!=DP.end())
		return DP[v];
	int ans=0;
	for (int i = 0; i < v.size(); ++i)
	{
		
	}
}
int main() {

	return 0;
}